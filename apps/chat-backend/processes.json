{
  "apps": [{
    "name": "api",
    "script": "./src/main.js",
    "merge_logs": true,
    "max_restarts": 20,
    "instances": "max",
    "exec_mode": "cluster",
    "max_memory_restart": "200M",
    "env": {
      "NODE_ENV": "production"
    }
  }]
}